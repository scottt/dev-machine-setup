Write a Python script, gsettings-apply, that takes a file path from the command line and apply it.
File contents would be like @darkmode.gsettings and @clock-24h.gsettings
Assume the 3rd argument in `.gsettings` files follow shell lexical convention regard to quoting
