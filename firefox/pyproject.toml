[project]
name = "firefox-add-on-policy-installer"
version = "0.1.0"
description = "Install Firefox add-ons with Firefox Enterprise Policies."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
