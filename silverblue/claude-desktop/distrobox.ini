# Distrobox manifest for the Claude Desktop container.
# The image is built locally from ./Containerfile (see claude-desktop-install).
# `exported_apps` places a launcher entry in the host GNOME app grid.
[claude]
image=localhost/claude-desktop:latest
pull=false
exported_apps="claude-desktop"
